"""
mother/application/__init__.py

Application layer for the Maternal Health bounded context.

Application services orchestrate between the domain layer and the
infrastructure (ORM/repositories).  They contain use-case logic but
no HTTP / view concerns.
"""
